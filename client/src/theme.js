import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffff',
      purple:'#ac2768',
      lightpurple:'#d6428b',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});

export default theme
